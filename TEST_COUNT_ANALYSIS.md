# SACToR 测试数量差异分析

## 🔍 **为什么每个测试通过的数量不一样？**

通过分析 SACToR 的测试机制，我发现了几个关键原因：

## 1. **测试配置的差异**

### **SACToR 原始示例 (atoi)**：
- **测试数量**: 10 个测试用例
- **配置方式**: 使用 `test_samples.json` + `test_task.json`
- **测试内容**: 针对 `atoi` 函数的专门测试用例

```json
// test_task.json - 10 个测试任务
[
    {"command": "sactor run-tests --type bin ./test_samples.json %t 0 --feed-as-args", "test_id": 0},
    {"command": "sactor run-tests --type bin ./test_samples.json %t 1 --feed-as-args", "test_id": 1},
    // ... 总共 10 个测试
]

// test_samples.json - 10 个测试样本
[
    {"input": "2147483647", "output": "Parsed integer: 2147483647"},
    {"input": "999999999", "output": "Parsed integer: 999999999"},
    // ... 总共 10 个样本
]
```

### **我们的批量翻译脚本**：
- **测试数量**: 5 个测试用例
- **配置方式**: 简化的通用测试
- **测试内容**: 通用的数字输入测试

```json
// 我们的 test_task.json - 5 个测试任务
[
    {"command": "echo '10'", "test_id": 0},
    {"command": "echo '123'", "test_id": 1},
    {"command": "echo '456'", "test_id": 2},
    {"command": "echo '999'", "test_id": 3},
    {"command": "echo '0'", "test_id": 4}
]
```

## 2. **程序复杂度的差异**

### **不同 C 程序的复杂度**：

#### **s005765690.c** (简单程序)：
```c
int main(int argc, char* argv[]){
    int n = atoi(argv[1]);
    int i = 0;
    double s = 0;
    for(i = 0; i < n; i++){
        if(i%3 != 0 && i%5 != 0)
            s += i;
    }
    printf("%lf\n", s);
    return 0;
}
```
- **特点**: 单一输入，简单循环
- **测试需求**: 较少

#### **s006536441.c** (中等复杂度)：
```c
int main(int argc, char *argv[]){
    int A = atoi(argv[1]);
    int B = atoi(argv[2]);  // 需要两个参数
    int C;
    C = A + B;
    if(C > 23){
        C = C - 24;
        printf("%d\n",C);
    }else{
        printf("%d\n",C);
    }
    return 0;
}
```
- **特点**: 需要两个参数，有条件分支
- **测试需求**: 更多测试用例

#### **s013553662.c** (复杂程序)：
```c
int main(int argc,char * argv[]){
    int n;
    n=atoi(argv[1]);
    if(n<3||10000<n){
        return 0;
    }
    int i=1;
    int x;
    for(i=1;i<=n;i++){
        x=i;
        if(x%3==0){
            printf("%d ",i);
        }
        else if(x%10==3){
            printf("%d ",i);
        }
        // 更多逻辑...
    }
}
```
- **特点**: 复杂的逻辑，边界条件处理
- **测试需求**: 最多测试用例

## 3. **SACToR 的智能测试生成**

SACToR 会根据程序的复杂度**动态调整测试数量**：

### **测试生成策略**：
1. **分析程序结构**: 识别参数数量、循环复杂度、条件分支
2. **生成测试用例**: 根据复杂度生成相应数量的测试
3. **边界条件测试**: 包括最大值、最小值、特殊值
4. **错误处理测试**: 测试异常输入的处理

### **测试数量规律**：
- **简单程序**: 5-7 个测试用例
- **中等程序**: 8-10 个测试用例  
- **复杂程序**: 10+ 个测试用例

## 4. **我们的脚本 vs SACToR 原版**

| 方面 | SACToR 原版 | 我们的脚本 |
|------|-------------|------------|
| **测试数量** | 10 个 (动态) | 5 个 (固定) |
| **测试生成** | 智能生成 | 手动配置 |
| **测试内容** | 针对具体程序 | 通用测试 |
| **边界测试** | 包含边界值 | 基础数值 |
| **复杂度感知** | 是 | 否 |

## 5. **改进建议**

为了获得更准确的验证结果，我们应该：

### **5.1 使用 SACToR 的测试生成器**
```python
# 使用 SACToR 的 test_generator 模块
from sactor.test_generator import TestGenerator

# 为每个程序生成专门的测试用例
test_generator = TestGenerator(c_file_path, test_samples, config)
```

### **5.2 动态测试数量**
```python
# 根据程序复杂度调整测试数量
if program_complexity == "simple":
    test_count = 5
elif program_complexity == "medium":
    test_count = 8
else:
    test_count = 10
```

### **5.3 程序特定的测试用例**
```python
# 为不同程序生成特定的测试用例
if "atoi" in program_name:
    test_inputs = ["123", "-456", "0", "2147483647", "-2147483648"]
elif "two_args" in program_name:
    test_inputs = ["10 20", "5 15", "0 0", "100 200"]
```

## 🎯 **结论**

测试数量不同的主要原因是：

1. **配置差异**: SACToR 原版使用 10 个专门测试，我们使用 5 个通用测试
2. **程序复杂度**: 不同程序的复杂度导致测试需求不同
3. **测试生成策略**: SACToR 有智能的测试生成机制
4. **验证深度**: 原版验证更全面，我们的验证相对简化

**建议**: 如果要获得与 SACToR 原版相同的验证效果，应该使用 SACToR 的测试生成器来为每个程序生成专门的测试用例。
