# SACToR 批量脚本修复总结

## 🔧 修复的问题

### 1. **路径问题**
- **问题**: test_task.json 中硬编码了 `/home/qsdrqs/` 路径，实际应该是 `/home/changdi/`
- **修复**: 使用绝对路径，避免路径硬编码问题

### 2. **文件复制问题**
- **问题**: 脚本试图复制 test_samples.json 到临时目录，容易出错
- **修复**: 直接使用原始绝对路径，不复制文件

### 3. **输出显示问题**
- **问题**: 使用 `capture_output=True` 导致看不到 SACToR 的详细输出
- **修复**: 移除 `capture_output=True`，显示完整的翻译过程

### 4. **超时设置问题**
- **问题**: 600秒超时太保守，导致批量处理很慢
- **修复**: 提供多个版本：
  - 标准版本: 600秒
  - 优化版本: 120秒
  - 超快速版本: 60秒

## ✅ 修复后的特性

### **直接使用绝对路径**
```python
# 修复前: 复制文件到临时目录
shutil.copy2(test_file_path, test_samples_path)

# 修复后: 直接使用原始路径
test_samples_path = test_file_path  # 直接使用原始路径
```

### **Docker 挂载优化**
```python
# 修复前: 复制到单一目录
"-v", f"{output_dir}:/tmp/translation"

# 修复后: 分别挂载不同目录
"-v", f"{os.path.dirname(c_file_path)}:/tmp/c_files",
"-v", f"{os.path.dirname(test_task_path)}:/tmp/test_tasks",
"-v", f"{output_dir}:/tmp/result"
```

### **显示完整输出**
```python
# 修复前: 捕获输出
result = subprocess.run(cmd, capture_output=True, text=True, timeout=600)

# 修复后: 显示输出
result = subprocess.run(cmd, text=True, timeout=600)
```

## 🚀 可用的批量脚本

### 1. **标准版本** (`batch_translate_correct.py`)
- 超时: 600秒
- 适用: 复杂程序
- 特点: 最稳定，成功率最高

### 2. **优化版本** (`batch_translate_optimized.py`)
- 超时: 120秒
- 适用: 中等复杂度程序
- 特点: 平衡速度和成功率

### 3. **超快速版本** (`batch_translate_ultra_fast.py`)
- 超时: 60秒
- 适用: 简单程序
- 特点: 最快速度，自动跳过复杂程序

## 📊 性能对比

| 版本 | 超时时间 | 预计时间/个 | 适用场景 |
|------|----------|-------------|----------|
| 标准版本 | 600秒 | 2-5分钟 | 复杂程序 |
| 优化版本 | 120秒 | 30-60秒 | 中等程序 |
| 超快速版本 | 60秒 | 15-30秒 | 简单程序 |

## 🎯 使用方法

```bash
# 标准版本
python3 batch_translate_correct.py

# 优化版本
python3 batch_translate_optimized.py

# 超快速版本
python3 batch_translate_ultra_fast.py
```

## ✅ 修复验证

修复后的脚本现在能够：
- ✅ 正确使用绝对路径
- ✅ 显示完整的 SACToR 输出
- ✅ 避免文件复制错误
- ✅ 支持多种超时设置
- ✅ 自动处理预生成测试用例
- ✅ 提供详细的进度信息
